char *string

int  m()
{
	return(printf("%s - %d, string, time()));
}

int main(int argc, char *argv)
{
	char *a;
	char *c;

	a = malloc(8);
	*a = 1;
	*(a + 4) = malloc(8);
	c = malloc(8);
	*c = 2;
	*(c + 4) = malloc(8);
	strcpy(a + 4, argv[1]);
	strcpy(c + 4, argv[2]);
	fgets(string ,0x44 ,fopen("/home/user/level8/.pass", "r"));
	puts("~~");
	return();
	
	
}
